#include<stdio.h>
int lcm(int ,int);
int main()
{
    printf("LCM is %d",lcm(15,12));
    return 0;
}
int lcm(int x,int y)
{
    int i;
     for(i=1;i<=x*y;i++)
     {
         if(i%x==0 && i%y==0)
         break;
     }

    return i;
}
